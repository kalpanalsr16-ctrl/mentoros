import { PlaceholderPage } from "@/design-system/layouts/PlaceholderPage";

/**
 * Dashboard content is explicitly out of Sprint 1's scope (Application
 * Shell only) — see docs/ui-architecture/13_Implementation_Sequence.md
 * Epic F, task F2 for the real Student Dashboard, built in a later,
 * separately-approved sprint.
 */
export default function StudentDashboardPage() {
  return (
    <PlaceholderPage
      title="Your dashboard"
      description="Your mastery snapshot, streak, and next steps will live here."
    />
  );
}
