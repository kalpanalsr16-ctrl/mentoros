import { PlaceholderPage } from "@/design-system/layouts/PlaceholderPage";

export default function CurriculumExplorerPage() {
  return (
    <PlaceholderPage
      title="Curriculum Explorer"
      description="Browse MentorOS's own curriculum alongside Learning Commons — coming in a later sprint."
    />
  );
}
